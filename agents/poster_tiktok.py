"""TikTok posting helper (Business API placeholder + automation fallback).

TikTok's upload APIs are gated; if you don't have Business API access you must use an automation fallback (browser automation) which may violate TOS. Use at your own risk.
"""

import os


def upload_to_tiktok(video_path: str, caption: str, api_key: str):
    # Placeholder: implement TikTok Business API upload flow or a documented automation fallback (Selenium/Playwright).
    raise NotImplementedError('TikTok upload is not implemented in this prototype. Provide TIKTOK Business API access or implement automation fallback.')
