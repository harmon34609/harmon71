#!/usr/bin/env bash
# Lightweight wrapper to run the agent without installing.
# Usage: ./bin/harmon71 create_post input.mp4 --caption "..."
HERE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHON=${PYTHON:-python3}
exec "$PYTHON" -m agents.agent "$@"
